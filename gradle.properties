# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# Daemon JVM. Raised from the template default for an 18-module build.
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8

# Multi-module build performance.
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=false

# Configuration cache is intentionally OFF for now; enable once KSP/Hilt/Room
# codegen is proven stable on this toolchain.
# org.gradle.configuration-cache=true

# AndroidX package structure.
android.useAndroidX=true

# Each library's R class contains only its own resources.
android.nonTransitiveRClass=true

# Kotlin code style for this project: "official" or "obsolete".
kotlin.code.style=official

# KSP incremental processing.
ksp.incremental=true
